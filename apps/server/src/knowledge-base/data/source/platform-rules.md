# HomeCraft platform rules (step0 placeholder)

- AI never decides geometry, compatibility, or price.
- Compatibility Engine is the only plan rejector.
- BOM uses frozen catalog snapshots per session.
