/**
 * Retrieves catalog modules relevant to a query via RAG.
 * Step0: returns empty list; indexer wired in phase 1.
 */
export async function retrieve(_query, _catalogId, _k = 5, _options = {}) {
  return [];
}
